#include<asgn2c.h>

using namespace std;

int main()
{
		  
    Rectangle rec;
    rec.SetLength(5);
    rec.SetWidth(2.5);
    rec.SetLength(5);
    rec.SetWidth(18.9);
    cout<<"\n\t\tRectangle \n";
    rec.Show();

   
    return 0;
}

